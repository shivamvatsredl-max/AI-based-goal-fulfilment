{
  "name": "dreamforge-monorepo",
  "version": "1.0.0",
  "description": "Dreamforge Workspace",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm --filter \"*\" build",
    "start": "pnpm --filter api-server start",
    "dev": "pnpm --filter \"*\" dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
